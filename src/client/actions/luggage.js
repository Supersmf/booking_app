import { createAction } from 'redux-actions';
import { ADD_LUGGAGE } from '../constants';


// eslint-disable-next-line import/prefer-default-export
export const addLuggage = createAction(ADD_LUGGAGE);
